{
  "name": "comb",
  "version": "0.1.0",
  "description": "Minimal code AND minimal words, combined. YAGNI/terse-prose persona plus a deterministic tool-output compressor, for Claude Code and Hermes.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
